let eleves = [
  { name: "Alice", hasReviewed: false },
  { name: "Bob", hasReviewed: false },
  { name: "Charlie", hasReviewed: false },
  { name: "David", hasReviewed: false },
];

for (i = 0; i < eleves.length; i + 1) {
  if ((eleves.hasReviewed = false)) eleves.hasReviewed = true;
}

function isReviewed(eleves) {
    return hasReviewed = false;
}

const chooseStudent = eleves.find((element) => hasReviewed = false);
chooseStudent.push(newArrayOfStudentsPicked);
newArrayOfStudentsPicked = [];

while (eleves.length > 0) {

}

function pickReviewer(eleves) {
  Math.floor(Math.random(eleves));
}

//Sélection aléatoire d'un élève.
//Assure que chaque élève est sélectionné au moins une fois avant qu'un élève puisse être choisi à nouveau.
//Suivi de l'état de sélection grâce à la propriété hasReviewed de chaque objet élève.

// Math random sur les objets du tableau eleves ; quand un objet est choisi, changé sa valeur a true (hasReviewed) 
//et le push dans un nouveau tableau. Faire la meme opération jusqu'a qu'il n'y est plus d'objet dans le premier tableau. 
//Si le tableau est vide, alors on prend un objet student aléatoirement a partir du deuxieme tableau.
